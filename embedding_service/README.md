# Why you need create this service
Because of Ollama doesn't have 
# Install and run embedding service
`python3 -m venv .venv`
`source .venv/bin/activate`
`pip install -r requirements.txt`
`python main.py`

