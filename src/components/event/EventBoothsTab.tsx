import React from 'react';

const EventBoothsTab: React.FC<any> = (props) => {
  return (
    <div>
      {/* ...existing code for booths tab... */}
    </div>
  );
};

export default EventBoothsTab;
